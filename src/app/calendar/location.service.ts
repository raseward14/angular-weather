import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class Location {
  // store a list of user clicked locations

  // needs to be able to remove location

  // needs to be able to add locations
}
