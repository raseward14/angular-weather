import { Component } from '@angular/core';

@Component({
  selector: 'app-location-list',
  imports: [],
  templateUrl: './location-list.html',
  styleUrl: './location-list.css'
})
export class LocationList {
  // reads from the location list service

  // prints those locations, and is updated whenever the location array updates

  // on location click, we need to get5DayForecast from the weather service
}
